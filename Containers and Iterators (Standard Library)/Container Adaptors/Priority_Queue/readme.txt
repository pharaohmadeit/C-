Priority_Queue Adaptor; heap data structure

-insertions in sorted order into the underlying data structure
-deletions from the front of the underlying data structure
-stores it elements in a vector
-elements compared with less<T> by default
-same functions as queue
