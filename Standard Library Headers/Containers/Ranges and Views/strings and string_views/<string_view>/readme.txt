string_view (lightweight views of contiguous characters)
-enables the writing of generic code that can handle any string-like type without the need for multiple overloads or conversions.
-provides a non-owning view of a string, meaning it doesn't allocate memory for its own copy. 
